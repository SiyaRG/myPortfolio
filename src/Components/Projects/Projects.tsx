import Pageload from "../Pageload";

const Projects = () => {
  return (
    <div id="Projects" style={{ height: "100vh" }}>
      <Pageload />
    </div>
  );
};

export default Projects;
