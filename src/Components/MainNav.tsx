import { motion } from "framer-motion";
const MainNav = () => {
  return (
    <div>
      <motion.nav>
        <motion.ul>
          <motion.li></motion.li>
        </motion.ul>
      </motion.nav>
    </div>
  );
};

export default MainNav;
